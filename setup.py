from setuptools import setup, find_packages
setup(
    name="llama3",  # Name of the package
    version='0.1.0',
    packages=find_packages(),  # Automatically find packages in the current directory
)